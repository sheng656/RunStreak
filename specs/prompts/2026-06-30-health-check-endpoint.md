# 2026-06-30 — Health Check Endpoint

**Prompt:**
> add a quick health-check endpoint to see if the app is booting successfully

**Output:**
Added a minimal API `GET /api/health` endpoint in `Program.cs` returning a JSON status to help verify backend uptime and boot success.
